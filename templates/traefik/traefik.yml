---
# Traefik v3.6 Static Configuration
# Managed by dockerHosting

entryPoints:
  web:
    address: ":80"
    http:
      redirections:
        entryPoint:
          to: websecure
          scheme: https
          permanent: true
    forwardedHeaders:
      trustedIPs:
      # __CLOUDFLARE_TRUSTED_IPS__
  websecure:
    address: ":443"
    forwardedHeaders:
      trustedIPs:
      # __CLOUDFLARE_TRUSTED_IPS__
  traefik:
    address: ":8080"

providers:
  file:
    directory: /etc/traefik/dynamic
    watch: true

api:
  dashboard: true
  insecure: false

log:
  level: INFO
  format: json

accessLog:
  format: json
