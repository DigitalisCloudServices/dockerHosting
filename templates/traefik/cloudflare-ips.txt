# Cloudflare published edge IP ranges.
#
# Traefik's forwardedHeaders.trustedIPs uses this list to decide which peers
# it trusts to set X-Forwarded-For / X-Forwarded-Proto: only Cloudflare's own
# edge, which terminates the client TLS connection and always sits directly
# in front of Traefik in this deployment. Without this, Traefik treats the
# Cloudflare edge itself as an untrusted client, discards the X-Forwarded-For
# header Cloudflare sets, and replaces it with the edge's own IP — so every
# downstream service (Kong) sees a Cloudflare PoP address instead of the real
# visitor, and per-IP rate limiting counts per PoP instead of per user.
#
# Source: https://www.cloudflare.com/ips-v4 and https://www.cloudflare.com/ips-v6
# Vendored: 2026-09-19
#
# One CIDR per line. Blank lines and lines starting with '#' are ignored by
# the installer's parser (scripts/install-traefik.sh: _render_trusted_ips_block).
# Re-vendor periodically — Cloudflare changes these ranges infrequently but
# without notice.

# IPv4
173.245.48.0/20
103.21.244.0/22
103.22.200.0/22
103.31.4.0/22
141.101.64.0/18
108.162.192.0/18
190.93.240.0/20
188.114.96.0/20
197.234.240.0/22
198.41.128.0/17
162.158.0.0/15
104.16.0.0/13
104.24.0.0/14
172.64.0.0/13
131.0.72.0/22

# IPv6
2400:cb00::/32
2606:4700::/32
2803:f800::/32
2405:b500::/32
2405:8100::/32
2a06:98c0::/29
2c0f:f248::/32
