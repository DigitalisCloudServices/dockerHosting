---
name: CI

on:
  push:
    branches: ["main"]
  pull_request:
    branches: ["main"]

jobs:
  lint:
    name: Lint (shellcheck)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Install shellcheck
        run: sudo apt-get update && sudo apt-get install -y shellcheck

      - name: Run shellcheck
        run: |
          shellcheck --severity=warning \
            setup.sh \
            deploy-site.sh \
            scripts/*.sh \
            lib/*.sh

  test-syntax:
    name: Syntax checks (bash -n)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Install bats-core
        run: |
          sudo apt-get update
          sudo apt-get install -y bats

      - name: Run syntax tests
        run: bats tests/test_syntax.bats

  test-arg-validation:
    name: Argument validation tests
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Install bats-core
        run: |
          sudo apt-get update
          sudo apt-get install -y bats

      - name: Run argument validation tests
        run: bats tests/test_arg_validation.bats

  test-traefik:
    name: Traefik script tests
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Install bats-core
        run: |
          sudo apt-get update
          # Use the upstream bats-core package for v1.x features (tap output, --recursive)
          sudo apt-get install -y bats || true
          # Fallback: install from GitHub release if system package is too old
          if ! bats --version | grep -qP 'Bats [1-9]'; then
            curl -fsSL https://github.com/bats-core/bats-core/archive/refs/tags/v1.11.0.tar.gz \
              | tar -xz -C /tmp
            sudo bash /tmp/bats-core-1.11.0/install.sh /usr/local
          fi

      - name: Run Traefik tests
        run: bats --recursive tests/traefik/

  test-lib:
    name: lib/ script tests
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Install bats-core
        run: |
          sudo apt-get update
          sudo apt-get install -y bats

      - name: Run lib/ tests
        run: bats --recursive tests/lib/

  test-pam-policy:
    name: PAM policy tests
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Install bats-core
        run: |
          sudo apt-get update
          sudo apt-get install -y bats

      - name: Run PAM policy tests
        run: bats tests/test_pam_policy.bats

  test-lifecycle-hooks:
    name: Lifecycle hook tests
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Install bats-core
        run: |
          sudo apt-get update
          sudo apt-get install -y bats

      - name: Run lifecycle hook tests
        run: bats tests/test_lifecycle_hooks.bats

  test-fail2ban:
    name: Fail2ban tests
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Install bats-core
        run: |
          sudo apt-get update
          # Use the upstream bats-core package for v1.x features (tap output, --recursive)
          sudo apt-get install -y bats || true
          # Fallback: install from GitHub release if system package is too old
          if ! bats --version | grep -qP 'Bats [1-9]'; then
            curl -fsSL https://github.com/bats-core/bats-core/archive/refs/tags/v1.11.0.tar.gz \
              | tar -xz -C /tmp
            sudo bash /tmp/bats-core-1.11.0/install.sh /usr/local
          fi

      - name: Run Fail2ban tests
        run: bats tests/security/test_fail2ban.bats

  test-harden-docker:
    name: Harden Docker tests
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Install bats-core
        run: |
          sudo apt-get update
          # Use the upstream bats-core package for v1.x features (tap output, --recursive)
          sudo apt-get install -y bats || true
          # Fallback: install from GitHub release if system package is too old
          if ! bats --version | grep -qP 'Bats [1-9]'; then
            curl -fsSL https://github.com/bats-core/bats-core/archive/refs/tags/v1.11.0.tar.gz \
              | tar -xz -C /tmp
            sudo bash /tmp/bats-core-1.11.0/install.sh /usr/local
          fi

      - name: Run Harden Docker tests
        run: bats tests/security/test_harden_docker.bats

  test-harden-kernel:
    name: Harden Kernel tests
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Install bats-core
        run: |
          sudo apt-get update
          # Use the upstream bats-core package for v1.x features (tap output, --recursive)
          sudo apt-get install -y bats || true
          # Fallback: install from GitHub release if system package is too old
          if ! bats --version | grep -qP 'Bats [1-9]'; then
            curl -fsSL https://github.com/bats-core/bats-core/archive/refs/tags/v1.11.0.tar.gz \
              | tar -xz -C /tmp
            sudo bash /tmp/bats-core-1.11.0/install.sh /usr/local
          fi

      - name: Run Harden Kernel tests
        run: bats tests/security/test_harden_kernel.bats

  test-install-packages:
    name: Install packages tests
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Install bats-core
        run: |
          sudo apt-get update
          # Use the upstream bats-core package for v1.x features (tap output, --recursive)
          sudo apt-get install -y bats || true
          # Fallback: install from GitHub release if system package is too old
          if ! bats --version | grep -qP 'Bats [1-9]'; then
            curl -fsSL https://github.com/bats-core/bats-core/archive/refs/tags/v1.11.0.tar.gz \
              | tar -xz -C /tmp
            sudo bash /tmp/bats-core-1.11.0/install.sh /usr/local
          fi

      - name: Run install packages tests
        run: bats tests/test_install_packages.bats

  test-scan-image:
    name: Scan image tests
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Install bats-core
        run: |
          sudo apt-get update
          # Use the upstream bats-core package for v1.x features (tap output, --recursive)
          sudo apt-get install -y bats || true
          # Fallback: install from GitHub release if system package is too old
          if ! bats --version | grep -qP 'Bats [1-9]'; then
            curl -fsSL https://github.com/bats-core/bats-core/archive/refs/tags/v1.11.0.tar.gz \
              | tar -xz -C /tmp
            sudo bash /tmp/bats-core-1.11.0/install.sh /usr/local
          fi

      - name: Run scan-image tests
        run: bats tests/test_scan_image.bats

  test-firewall:
    name: Firewall tests
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Install bats-core
        run: |
          sudo apt-get update
          sudo apt-get install -y bats || true
          if ! bats --version | grep -qP 'Bats [1-9]'; then
            curl -fsSL https://github.com/bats-core/bats-core/archive/refs/tags/v1.11.0.tar.gz \
              | tar -xz -C /tmp
            sudo bash /tmp/bats-core-1.11.0/install.sh /usr/local
          fi

      - name: Run firewall tests
        run: bats tests/security/test_firewall.bats

  test-run-report:
    name: Run-report tests
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Install bats-core
        run: |
          sudo apt-get update
          sudo apt-get install -y bats || true
          if ! bats --version | grep -qP 'Bats [1-9]'; then
            curl -fsSL https://github.com/bats-core/bats-core/archive/refs/tags/v1.11.0.tar.gz \
              | tar -xz -C /tmp
            sudo bash /tmp/bats-core-1.11.0/install.sh /usr/local
          fi

      - name: Run run-report tests
        run: bats tests/test_run_report.bats

  test-install-observability:
    name: Install observability tests
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Install bats-core
        run: |
          sudo apt-get update
          sudo apt-get install -y bats || true
          if ! bats --version | grep -qP 'Bats [1-9]'; then
            curl -fsSL https://github.com/bats-core/bats-core/archive/refs/tags/v1.11.0.tar.gz \
              | tar -xz -C /tmp
            sudo bash /tmp/bats-core-1.11.0/install.sh /usr/local
          fi

      - name: Run install-observability tests
        run: bats tests/test_install_observability.bats

  test-configure-observability-egress:
    name: Configure observability egress tests
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Install bats-core
        run: |
          sudo apt-get update
          sudo apt-get install -y bats || true
          if ! bats --version | grep -qP 'Bats [1-9]'; then
            curl -fsSL https://github.com/bats-core/bats-core/archive/refs/tags/v1.11.0.tar.gz \
              | tar -xz -C /tmp
            sudo bash /tmp/bats-core-1.11.0/install.sh /usr/local
          fi

      - name: Run configure-observability-egress tests
        run: bats tests/test_configure_observability_egress.bats

  test-yaml:
    name: YAML validation
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Install yamllint
        run: sudo apt-get update && sudo apt-get install -y yamllint

      - name: Run YAML validation
        run: |
          yamllint -c .yamllint.yml \
            templates/traefik/traefik.yml \
            templates/traefik/middleware.yml \
            .github/workflows/ci.yml

      - name: Validate site.yml.template after substitution
        run: >
          sed 's/{{[A-Z_]*}}/placeholder/g' templates/traefik/site.yml.template
          | yamllint -c .yamllint.yml -
